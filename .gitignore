# Секреты — им тут не место (см. CLAUDE.md), храним только в Downloads\.env
.env
*.env

# Локальные бэкапы — история теперь в git, ручные .bak-копии не нужны
*.bak
*.bak-*
*.bak.*

# Логи сервера предпросмотра (docs/serve-ramps.sh)
*.log
