# Открытые задачи

Плоский список того, что известно как незакрытое, но не является активным
статусом на сейчас (для этого - `printer-status.md`). Не GitHub Issues (`gh`
не установлен/не настроен в этом окружении, в проекте нет GitHub Issues
workflow) - обычный markdown, как всё остальное в `docs/`.

## Экран/KlipperScreen

- [x] Экран гас навсегда через ~1ч простоя (DPMS Suspend:3600 по умолчанию у
  X, KlipperScreen не переоткрывает подсветку) - обнаружено живьём 2026-07-30
  (пользователь вернулся к чёрному экрану). Исправлено:
  `orangepi-display/xorg.conf.d/20-ili9341.conf` теперь явно глушит
  Standby/Suspend/Off/BlankTime в ServerFlags. Задеплоено и проверено
  переживающим `systemctl restart KlipperScreen`.
- [x] Шрифт/иконки - `font_size: medium` + тема `big-font` (не `max`, см.
  комментарий в `printer-configs/KlipperScreen.conf`). Подтверждено живым
  скриншотом: подписи Extruder/Heater Bed/OrangePi CPU видны полностью, без
  скролбара, иконки на месте.
- [x] Переполнение цифровой клавиатуры вправо за край экрана -
  `orangepi-display/klipperscreen-patches/keypad-width-overflow.patch`,
  снят с живой правки на принтере, README рядом объясняет как переприменять
  после обновления KlipperScreen.
- [x] "Move Distance (mm)" на панели Move обрезался снизу экрана (значения
  1/5/10/25/50 не читались вообще) - `.horizontal_togglebuttons` в
  `base.css` даёт `min-height: 2.9em`, при 14px это ~41px, столько же не
  хватало по вертикали на 240px экране. Фикс в
  `printer-configs/KlipperScreen-themes/big-font/style.css` (та же техника,
  что уже применялась к `.heater-grid button`). Подтверждено живым
  скриншотом - значения читаются.
- [x] Индикатор фокуса вообще не был виден ни на одной кнопке (репорт
  пользователя: "фокус перестал показываться") - тема `big-font` писалась с
  нуля и не унаследовала `button:focus { background-color: @active }` из
  `styles/z-bolt/style.css`; `base.css` сам по себе стилизует только
  `button:active`/`.button_active` (клик/вручную-переключаемый класс), не
  клавиатурный фокус. Добавлено в тему.
- [x] По просьбе пользователя фокус теперь гаснет через 10с простоя после
  последнего нажатия (`orangepi-display/klipperscreen-patches/focus-clear-timeout.patch`,
  таймер в `screen.py`). Проверен полный цикл: нажатие -> подсветка
  появляется -> 10с простоя -> подсветка гаснет -> нажатие -> подсветка
  снова появляется на разумном элементе.
- [ ] Кнопка "Print" в правом столбце главного экрана обрезается
  ("Pri", иконка наполовину) - подтверждено и Fable judge-panel, и глазами.
  НЕ исправлено: пробовал уменьшить padding у `button.color1-4` - стало
  ХУЖE (кнопка перестала быть видна вообще, а не частично - её собственная
  рамка влезла в экран, но иконка+текст внутри перестали помещаться в неё),
  фикс отменён и не входит в текущую версию темы. Диагностика через
  временные отладочные рамки (`button.color1` -> красная и т.п., НЕ
  коммитить, вызывает `gtk-css-provider-error` при `!important` - GTK CSS
  не поддерживает эту синтаксическую конструкцию, роняет весь KlipperScreen
  на старте) показала 3 полноширинных прямоугольника (Move=красный,
  Extrude=голубой, Print=красный/пустой) вместо ожидаемых по коду 2 колонок
  (`main_menu.py` вызывает `arrangeMenuItems(items, 2, True)`, при 5
  включённых пунктах меню (`move`,`temperature`,`extrude`,`more`,`print` -
  `gcodes` выключен, т.к. `extruders.count>0`) должно быть move+temperature
  в ряд 0, extrude+more в ряд 1, print на всю ширину в ряд 2 - "temperature"
  и "more" нигде не видны, ни отдельным цветом, ни хоть какой-то шириной).
  `vertical_mode = aspect_ratio < 1.0` = `False` (320/240=1.33) - подтверждено
  чтением `screen.py:149`, так что `AutoGrid`'s `vertical`-ветка (которая
  форсирует 1 колонку) НЕ должна срабатывать - противоречие с наблюдаемой
  одноколоночной раскладкой не разрешено. Низкий приоритет на практике:
  "Print" открывает панель gcodes, а печать сейчас всё равно невозможна
  (экструдер разобран, хоуминг X не доверенный - см. `printer-status.md`).
  Если возвращаться - начать с этого противоречия, а не с новых догадок по
  padding.
- [ ] `Enter`/`Return` не активировал панель уведомлений с фокуса на
  колокольчике в одной проверке 2026-07-30 (после патча focus-clear-timeout,
  но патч фокуса не трогает активацию кнопок - похоже на существовавший
  раньше нюанс). Не разобрано, не подтверждено как регрессия именно от
  сегодняшних правок.
- [ ] Мелкие косметические находки Fable judge-panel, не исправлялись
  (низкий приоритет): подписи осей графика температур ~6-7px, менее читаемы
  чем остальной текст 14px; перенос длинной строки в Notifications не имеет
  отступа под колонку сообщения (первая строка лога начинается от левого
  края, а не от колонки текста); кнопка "Clear" в Notifications выглядит
  как голый текст с еле заметной точкой вместо иконки; неактивные
  back/menu-grid иконки на главном экране затемнены почти до
  невидимости (это штатное disabled-состояние GTK, не баг, но контраст
  можно повысить).

## Скрипты

- [ ] `scripts/deploy.sh`: `read -r -p "Продолжить?"` виснет/молча падает при
  неинтерактивном запуске (`echo y | bash scripts/deploy.sh`) - `ssh`-вызовы
  внутри собственного diff-цикла скрипта потребляют переданный в pipe stdin
  раньше, чем до него доходит `read`. Не чинилось в этой сессии - деплои
  делались вручную (backup + scp + chown) в обход самого скрипта. Чинить:
  либо `read ... < /dev/tty`, либо `ssh -n`/`</dev/null` на diff-вызовах
  внутри скрипта, чтобы не есть stdin родителя.

## UX judge-panel (Fable)

_Заполняется автоматически после прогона judge-panel воркфлоу
(`Goal set: dynamic workflow + fable` от 2026-07-30) - см. ниже в этом же
файле после соответствующего раздела, если он есть, иначе воркфлоу ничего
серьёзного не нашёл._
