--- a/panels/temperature.py
+++ b/panels/temperature.py
@@ -46,22 +46,56 @@
         self.content.add(self.grid)
 
     def create_right_panel(self):
+        # 2026-07-31 right-panel overflow fix (user report: "Cooldown" label
+        # clipped at the screen's right edge, no ellipsis -- see
+        # orangepi-display/klipperscreen-patches/README.md for the full
+        # live-measurement writeup). Root cause: this used to be a single
+        # Gtk.Grid(column_homogeneous=True) with `cooldown` spanning 2 of 3
+        # columns and the preheat/delta_adjust widget spanning all 3 on the
+        # row below. A widget spanning multiple columns in a homogeneous
+        # Gtk.Grid inflates EVERY column's minimum-size computation to its
+        # own (undivided) minimum rather than minimum/span -- confirmed live
+        # (get_preferred_width() logging, since removed): the grid reported
+        # minimum_width == natural_width == 141px regardless of how the
+        # inner content was restructured, well beyond the ~79px actually
+        # available (320px screen - ~36px sidebar - 205px left device list,
+        # the latter a hard floor from `.heater-grid button {min-width:
+        # 6.5em}` used elsewhere to keep "OrangePi CPU" from clipping).
+        # Switching to two plain Gtk.Box containers (a horizontal one for
+        # cooldown+adjust, a vertical one stacking that atop the preheat/
+        # delta widget) sidesteps the homogeneous-grid-with-span pathology
+        # entirely -- Box sizing is a simple sum/max over children with no
+        # span-related inflation, and correctly shrinks down toward each
+        # child's real minimum when space is tight, letting Pango ellipsize
+        # properly instead of drawing past the window edge.
+        #
+        # cooldown's label is intentionally dropped (label=None, icon-only --
+        # same idiom already used for `adjust` right next to it, and for the
+        # dashboard's bottom nav row per dashboard-icon-only-rows.patch).
+        # With a label, ellipsize-END *did* correctly stop the text from
+        # running past the screen edge once the Box refactor below gave it a
+        # real (small) allocation instead of an inflated one, but the
+        # available width only left room for the icon plus literal "..." --
+        # worse than no text at all. Icon-only reads cleanly at this budget
+        # and matches the sibling button.
         cooldown = self._gtk.Button(
-            "cool-down", _("Cooldown"), "color4", self.bts, Gtk.PositionType.LEFT, 1
+            "cool-down", None, "color4", self.bts, Gtk.PositionType.LEFT, 1
         )
         adjust = self._gtk.Button(
             "fine-tune", None, "color3", self.bts * 1.4, Gtk.PositionType.LEFT, 1
         )
         cooldown.connect("clicked", self.set_temperature, "cooldown")
         adjust.connect("clicked", self.switch_preheat_adjust)
+        cooldown.set_hexpand(True)
+        adjust.set_hexpand(False)
 
-        right = Gtk.Grid(row_homogeneous=True, column_homogeneous=True)
-        right.attach(cooldown, 0, 0, 2, 1)
-        right.attach(adjust, 2, 0, 1, 1)
-        if self.show_preheat:
-            right.attach(self.preheat(), 0, 1, 3, 2)
-        else:
-            right.attach(self.delta_adjust(), 0, 1, 3, 2)
+        top_row = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL, spacing=0)
+        top_row.pack_start(cooldown, True, True, 0)
+        top_row.pack_start(adjust, False, False, 0)
+
+        right = Gtk.Box(orientation=Gtk.Orientation.VERTICAL, spacing=0)
+        right.pack_start(top_row, False, False, 0)
+        right.pack_start(self.preheat() if self.show_preheat else self.delta_adjust(), True, True, 0)
         return right
 
     def switch_preheat_adjust(self, widget):
@@ -76,23 +110,75 @@
         self.grid.show_all()
 
     def preheat(self):
+        # 2026-07-31 right-panel overflow fix, continued: was a 2-column grid
+        # (PLA|ABS / PETG|FLEX), which on the ~79px budget available here
+        # left each button ~35px wide -- entire second column (ABS/FLEX) was
+        # rendering past the screen's right edge, fully invisible, same bug
+        # as the "Cooldown" clipping above. Single column instead: each
+        # preset button gets the full available width (still tight enough to
+        # wrap PLA/ABS/PETG/FLEX onto two lines rather than fit on one, but
+        # every character stays on-screen and reachable), the extra vertical
+        # length scrolls via the existing ScrolledWindow instead of needing
+        # horizontal room. The scroll's own width fix is below.
         self.labels["preheat_grid"] = Gtk.Grid(row_homogeneous=True, column_homogeneous=True)
         i = 0
         for option in self.preheat_options:
             if option != "cooldown":
                 self.labels[option] = self._gtk.Button(label=option, style=f"color{(i % 4) + 1}")
                 self.labels[option].connect("clicked", self.set_temperature, option)
-                self.labels["preheat_grid"].attach(self.labels[option], (i % 2), int(i / 2), 1, 1)
+                self.labels["preheat_grid"].attach(self.labels[option], 0, i, 1, 1)
                 i += 1
         scroll = self._gtk.ScrolledWindow()
+        # Without this, CustomScrolledWindow's own preferred-width query
+        # (confirmed live) reported minimum_width == natural_width == 141px
+        # regardless of preheat_grid's actual (much smaller) content width --
+        # a GtkScrolledWindow reserves room for a possible horizontal
+        # scrollbar/natural-width propagation unless told not to. NEVER
+        # forecloses horizontal scrolling entirely (not needed, single column
+        # now), and the two width calls pin the scrolled window to its
+        # child's real minimum instead of that inflated floor.
+        scroll.set_policy(Gtk.PolicyType.NEVER, Gtk.PolicyType.AUTOMATIC)
+        scroll.set_min_content_width(1)
+        scroll.set_propagate_natural_width(False)
+        # THE decisive one for legibility (2026-07-31, second pass after a
+        # review caught the labels still wrapping as "P-"/"LA"): this is a
+        # CustomScrolledWindow (ks_includes/widgets/scroll.py), which sets
+        # `overlay_scrolling: False` by default -- so its vertical scrollbar
+        # takes REAL layout width out of the ~77px this column gets, leaving
+        # preheat_grid just 30px against a 49px natural width (measured live).
+        # At 30px, Pango falls back to WORD_CHAR character-level wrapping and
+        # "PLA" renders as "P-" / "LA" on two lines. base.css sizes that
+        # scrollbar `scrollbar slider {min-width: 2.5em}` == ~35px at this
+        # theme's 14px font -- nearly half the column. Overlay scrolling draws
+        # it on top of the content instead of beside it, costing zero layout
+        # width; identical reasoning and identical fix to the one already
+        # applied to the main menu (main-menu-single-column.patch), and the
+        # auto-hide behaviour it brings is a non-issue on a D-pad-only kiosk
+        # with no mouse hover.
+        scroll.set_overlay_scrolling(True)
         scroll.add(self.labels["preheat_grid"])
         return scroll
 
     def delta_adjust(self):
         # Create buttons for increase and decrease
-        self.labels["increase"] = self._gtk.Button("increase", None, "color1")
+        # 2026-07-31 landscape overflow fix, the binding constraint (found by
+        # live get_preferred_width() logging, since removed -- the 2x2
+        # tempgrid below only needs 56px, so it was never the problem):
+        # KlippyGtk.Button() multiplies its icon scale by an extra 1.4 when
+        # `label is None`, i.e. exactly for icon-only buttons like these two.
+        # That made each -/+ button 42px minimum, so the pair needed 84px
+        # against the 79px this column actually gets -- a 5px deficit that
+        # kept the row (and everything right of it) hanging off the screen.
+        # Passing button_image_scale / 1.4 cancels that icon-only bonus, the
+        # exact idiom already used for the dashboard's device rows in
+        # dashboard-icon-only-rows.patch. Icons land at the same visual size
+        # as the rest of the UI's icons rather than oversized, and the pair
+        # drops to ~70px. Landscape-only concern, but the scale is harmless
+        # in portrait too, so it is not branched.
+        icon_scale = self._gtk.button_image_scale / 1.4
+        self.labels["increase"] = self._gtk.Button("increase", None, "color1", icon_scale)
         self.labels["increase"].connect("clicked", self.change_target_temp_incremental, "+")
-        self.labels["decrease"] = self._gtk.Button("decrease", None, "color3")
+        self.labels["decrease"] = self._gtk.Button("decrease", None, "color3", icon_scale)
         self.labels["decrease"].connect("clicked", self.change_target_temp_incremental, "-")
 
         # Create buttons for temperature deltas
@@ -106,21 +192,61 @@
 
         # Create grid for temperature deltas
         tempgrid = Gtk.Grid()
-        for j, i in enumerate(self.tempdeltas):
-            tempgrid.attach(self.labels[f"deg{i}"], j, 0, 1, 1)
+        if self._screen.vertical_mode:
+            for j, i in enumerate(self.tempdeltas):
+                tempgrid.attach(self.labels[f"deg{i}"], j, 0, 1, 1)
+        else:
+            # 2026-07-31 landscape overflow fix: 2x2 instead of 1x4. Four
+            # delta buttons side by side in the ~79px this column gets is
+            # ~20px each, which cannot hold "25" plus the
+            # .horizontal_togglebuttons border -- measured live, the row
+            # overflowed and "25" was off-screen entirely. 2x2 gives each
+            # button ~39px, comfortably enough, and the extra row costs
+            # height this column has to spare (the delta stack is much
+            # shorter than the preheat list it replaces).
+            for j, i in enumerate(self.tempdeltas):
+                tempgrid.attach(self.labels[f"deg{i}"], j % 2, j // 2, 1, 1)
 
-        # Create grid for decrease button, increase button, temperature labels, and grid
-        deltagrid = Gtk.Grid(row_homogeneous=True, column_homogeneous=True)
         if self._screen.vertical_mode:
+            # Portrait branch unchanged -- it has the full screen width and
+            # none of the landscape height/width pressure.
+            deltagrid = Gtk.Grid(row_homogeneous=True, column_homogeneous=True)
             deltagrid.attach(self.labels["decrease"], 0, 1, 1, 1)
             deltagrid.attach(self.labels["increase"], 1, 1, 1, 1)
             deltagrid.attach(tempgrid, 0, 2, 2, 1)
-        else:
-            deltagrid.attach(self.labels["decrease"], 0, 1, 1, 3)
-            deltagrid.attach(self.labels["increase"], 1, 1, 1, 3)
-            deltagrid.attach(Gtk.Label(_("Temperature") + " (°C)"), 0, 4, 2, 1)
-            deltagrid.attach(tempgrid, 0, 5, 2, 2)
-        return deltagrid
+            return deltagrid
+
+        # 2026-07-31 landscape overflow fix (the sibling of the preheat fix
+        # in create_right_panel above -- same panel, the OTHER branch, which
+        # you reach via the sliders button next to Cooldown, and which
+        # becomes the DEFAULT view while printing/paused since show_preheat
+        # is `state not in ("printing", "paused")`).
+        #
+        # Was a Gtk.Grid(column_homogeneous=True) with two children spanning
+        # both columns: a raw Gtk.Label("Temperature (°C)") and tempgrid. Per
+        # the same GTK pathology documented in create_right_panel, a
+        # column-spanning child inflates EVERY column's minimum to its own
+        # undivided minimum -- and that label is ~16 characters (~110-120px
+        # at this theme's 14px font) against the ~79px actually available.
+        # Measured live before the fix: this widget reported
+        # minimum_width == natural_width == 122px and was allocated
+        # x=241..363 on a 320px screen -- the "+" button, the "25" delta, the
+        # sliders button itself and even the titlebar clock were all pushed
+        # off the right edge.
+        #
+        # Plain Gtk.Box instead (sum/max sizing, no span inflation), and the
+        # "Temperature (°C)" caption is dropped: it is redundant here -- the
+        # panel is already titled "Temperature" and the device list carries a
+        # "Temp (°C)" column header two inches to the left. Same call, for
+        # the same reason, as dropping "Move Distance (mm)" in
+        # move-panel-bottom-clipping.patch.
+        deltabox = Gtk.Box(orientation=Gtk.Orientation.VERTICAL, spacing=0)
+        pm_row = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL, spacing=0)
+        pm_row.pack_start(self.labels["decrease"], True, True, 0)
+        pm_row.pack_start(self.labels["increase"], True, True, 0)
+        deltabox.pack_start(pm_row, True, True, 0)
+        deltabox.pack_start(tempgrid, False, False, 0)
+        return deltabox
 
     def change_temp_delta(self, widget, tempdelta):
         logging.info(f"### tempdelta {tempdelta}")
--- a/panels/system.py
+++ b/panels/system.py
@@ -175,12 +175,36 @@
         self.cpu_flowbox = Gtk.FlowBox(
             selection_mode=Gtk.SelectionMode.NONE,
             homogeneous=True,
-            column_spacing=32,
+            # 2026-07-31 overflow fix, continued: column_spacing 32 -> 10.
+            # halign FILL + hexpand True (both still needed -- see below) had
+            # FlowBox stretch its 2 (now-capped) columns across the entire
+            # width the grid handed it, and with 32px between them that put
+            # the 2nd column's right edge past x=320 (confirmed live: capping
+            # to 2-per-line alone only fixed the *count*, a follow-up
+            # screenshot still showed the 2nd core's value clipped to a
+            # single bracket-like glyph at the screen edge). Each label is a
+            # few characters ("  0%".."100%"); it doesn't need anywhere near
+            # 32px of daylight between columns on a 320px screen.
+            column_spacing=10,
             row_spacing=0,
             halign=Gtk.Align.FILL,
             hexpand=True,
         )
         self.cpu_flowbox.set_min_children_per_line(1)
+        # 2026-07-31 overflow fix (user report, live screenshot): with no
+        # max_children_per_line set, Gtk.FlowBox picked 3-per-line on this
+        # panel's ~150-160px column (column 0 -- "Cores:" -- and the sidebar
+        # eat most of the 320px screen), but 3 "  0%"-style labels plus two
+        # 32px column_spacing gaps don't actually fit that budget -- the 3rd
+        # core's percentage ran off the right edge of the screen (visible on
+        # a live screenshot as a single clipped glyph), leaving only 2 fully
+        # readable per line anyway and 1 wrapped alone to its own line below.
+        # Capping at 2 makes FlowBox commit to the width that's actually
+        # available instead of guessing 3 and clipping the overflow -- a
+        # clean 2-wide grid for any core count (Orange Pi One is quad-core,
+        # so this is 2 rows of 2 here; more cores just add more rows, which
+        # is fine, this whole section already scrolls).
+        self.cpu_flowbox.set_max_children_per_line(2)
         self.cpu_flowbox.get_style_context().add_class("monospaced")
 
         for i in range(self.cpu_count):
@@ -209,6 +233,26 @@
         self.populate_info()
 
         scroll = self._gtk.ScrolledWindow()
+        # 2026-07-31 overflow fix, THE decisive lever (found by live
+        # get_allocation() logging, since removed -- guessing at spacing
+        # constants first was not enough, see the two notes above). This is a
+        # CustomScrolledWindow (ks_includes/widgets/scroll.py) with
+        # `overlay_scrolling: False` by default, so its vertical scrollbar --
+        # base.css `scrollbar slider {min-width: 2.5em}` == ~35px at this
+        # theme's 14px font -- eats real layout width out of the ~284px
+        # content area (320px screen minus the ~36px sidebar), leaving the
+        # viewport only ~249px. This panel is long and always scrolls, so
+        # that scrollbar is ALWAYS present. Measured: self.grid's minimum
+        # width is 273px (the "Cores:" label column 97px + the core-percent
+        # FlowBox 166px + spacing), and GTK honours a widget's minimum over
+        # the space actually available, so the grid was allocated its full
+        # 273px and simply got clipped by the 249px viewport -- cutting the
+        # right-hand core column mid-glyph, exactly as reported. Overlay
+        # scrolling draws the scrollbar on top of the content instead of
+        # beside it, returning those ~35px to the viewport (284px > 273px, so
+        # the grid now fits with room to spare). Same fix, same reason, as
+        # main-menu-single-column.patch and the Temperature preset column.
+        scroll.set_overlay_scrolling(True)
         scroll.add(self.grid)
         self.content.add(scroll)
 
@@ -234,7 +278,16 @@
     def populate_info(self):
         # date
         self.add_label_to_grid(self.prettify("date"), 0, bold=True)
-        self.labels["date"] = Gtk.Label(label="", xalign=0)
+        # 2026-07-31 overflow fix (user report, live screenshot): unlike
+        # every value label built through add_label_to_grid() above (which
+        # already sets wrap=True + WORD_CHAR), this one was constructed
+        # directly with neither -- a full `now.strftime("%a %b %e %H:%M:%S
+        # %Z %Y")` string ("Fri Jul 31 01:39:50 MSK 2026") is long enough
+        # that it ran off the right edge of the 320px screen, cutting off
+        # the year. wrap=True + WORD_CHAR (matching add_label_to_grid's
+        # convention exactly) lets it break onto a second line instead.
+        self.labels["date"] = Gtk.Label(label="", xalign=0, wrap=True)
+        self.labels["date"].set_line_wrap_mode(Pango.WrapMode.WORD_CHAR)
         self.grid.attach(self.labels["date"], 1, self.current_row - 1, 1, 1)
         self.add_label_to_grid("", 0)
 
--- a/panels/console.py
+++ b/panels/console.py
@@ -43,9 +43,27 @@
         options.attach(o2_button, 1, 0, 1, 1)
 
         sw = Gtk.ScrolledWindow(hexpand=True, vexpand=True)
+        # 2026-07-31 overflow fix (user report, live screenshot): Gtk.TextView
+        # defaults to Gtk.WrapMode.NONE, so any gcode response/echo line wider
+        # than the 320px screen (e.g. "Must home axis first: 25.000000...")
+        # just ran off the right edge with no way to read the rest -- the
+        # ScrolledWindow's horizontal scrollbar defaults to overlay mode on
+        # this theme (see main-menu-single-column.patch's
+        # set_overlay_scrolling(True) discussion) and auto-hides, and there is
+        # no touchscreen/mouse on this kiosk to hover-then-drag it into view,
+        # so the clipped tail was permanently unreachable, not just visually
+        # tight. WORD_CHAR wraps normally on word boundaries but still breaks
+        # a single unbroken run of characters if needed (long numbers/paths),
+        # matching the same mode already used for Notifications
+        # (hanging-indent-notifications.patch). NEVER on the horizontal
+        # scrollbar policy removes the now-pointless empty affordance and
+        # guarantees wrapping is what actually happens instead of a
+        # zero-height scroll range some GTK versions could substitute.
+        sw.set_policy(Gtk.PolicyType.NEVER, Gtk.PolicyType.AUTOMATIC)
 
         tb = Gtk.TextBuffer()
         tv = Gtk.TextView(buffer=tb, editable=False, cursor_visible=False)
+        tv.set_wrap_mode(Gtk.WrapMode.WORD_CHAR)
         tv.connect("size-allocate", self._autoscroll)
         tv.connect("touch-event", self._screen.remove_keyboard)
         tv.connect("button-press-event", self._screen.remove_keyboard)
--- a/styles/base.css
+++ b/styles/base.css
@@ -38,9 +38,48 @@
  * Gtk.Grid is used ~70 other places in the codebase (some nested inside
  * intentionally differently-colored ancestors) and job_status.py has its
  * own Gtk.Overlay for a progress indicator over a thumbnail image, where
- * painting an opaque @bg would visibly break that overlay. */
+ * painting an opaque @bg would visibly break that overlay.
+ *
+ * 2026-07-31 follow-up: .heatergraph added to this list. Same root cause,
+ * different widget -- HeaterGraph (ks_includes/widgets/heatergraph.py) is a
+ * Gtk.DrawingArea, which owns its own native GdkWindow same as main_grid
+ * did, and draw_graph() repaints it each frame via
+ * `Gtk.render_background(style_context, ctx, ...)` using ITS OWN style
+ * context. With no background-color rule matching '.heatergraph', that
+ * resolves to transparent and renders solid black on this compositor-less
+ * X11/DRM setup -- invisible on the embedded dashboard/Temperature-panel
+ * graph (its container, main_grid, already painted @bg behind it after the
+ * fix above, showing through where the graph doesn't happen to overpaint),
+ * but fully exposed in the fullscreen graph dialog
+ * (show_fullscreen_graph()/KlippyGtk.Dialog()), where this DrawingArea is
+ * the dialog's only real content and there is no @bg-painted ancestor
+ * behind it -- confirmed live, whole dialog rendered black except the
+ * explicitly-drawn grid lines/axis text/curves.
+ *
+ * 2026-07-31 second follow-up: every Gtk.Dialog built by KlippyGtk.Dialog()
+ * (Save Config confirm, PID Calibrate confirm, every _confirm_send_action
+ * popup) -- same root cause again, confirmed by pixel-sampling a live
+ * screenshot of the "Save configuration?" dialog: (0,0,0) pure black at
+ * the corners/margins AND behind the message text, not this theme's @bg or
+ * @text-inv. Each such dialog is a fresh top-level GdkWindow realized at
+ * runtime (unlike the one persistent KlipperScreen window mapped at
+ * startup, which the original diagnosis above confirmed resolves @bg
+ * correctly) -- on this compositor-less raw X11/DRM setup a newly realized
+ * surface starts unpainted. Fixed on content_area (see
+ * ks_includes/KlippyGtk.py's Dialog(), class app_dialog_content) rather
+ * than the dialog window itself: content_area is the Gtk.Box that actually
+ * spans the dialog's visible area (same idiom as main_grid inside
+ * screen.py's overlay), and painting it directly sidesteps needing the
+ * window-level '.dialog'/'window' rules below to themselves resolve
+ * correctly on a freshly-realized surface. Deliberately @bg, not the
+ * '.dialog' rule's @text-inv "card" color below -- @bg keeps this fallback
+ * visually consistent with every other panel/dialog on the device instead
+ * of introducing a second, dialog-only background color that only shows up
+ * on the rendering path that was broken to begin with. */
 .main_overlay,
 .main_grid,
+.heatergraph,
+.app_dialog_content,
 list row,
 treeview.view,
 window {
@@ -322,11 +361,54 @@
     padding: 0.25em 2.5em;
 }
 
+/* 2026-07-31 black-background fix, dialog case (user report, live
+ * screenshot: the "Save configuration?" confirm dialog rendered solid
+ * black). Root cause, same family as .main_overlay/.main_grid/.heatergraph
+ * above: every Gtk.Dialog built by KlippyGtk.Dialog() (Save Config confirm,
+ * PID Calibrate confirm, every _confirm_send_action popup) is a fresh
+ * top-level GdkWindow realized at *runtime*, unlike the one persistent
+ * KlipperScreen window mapped at startup that the original diagnosis above
+ * confirmed does resolve @bg correctly. On this compositor-less raw
+ * X11/DRM setup, that runtime-realized surface never paints its
+ * CSS-declared background no matter what selector targets the window node
+ * itself -- confirmed by pixel-sampling four separate live screenshots:
+ * neither `window {background-color: @bg}` above nor `.dialog`'s own
+ * `background-color: @text-inv` below ever painted anything on the actual
+ * GtkWindow, class name or not.
+ *
+ * The fix that actually worked: paint content_area (the Gtk.Box that
+ * holds the dialog's real content, see KlippyGtk.py's Dialog(), class
+ * app_dialog_content, background-color: @bg in the shared rule above) --
+ * same idiom as main_grid inside screen.py's overlay, sidestepping the
+ * broken window-level paint entirely rather than trying to fix it. That
+ * alone left a black ring around the card exactly the size of the layout
+ * gap between content_area and the window edge (GTK margins/padding are
+ * empty space OUTSIDE a widget's own painted rectangle -- they're the
+ * PARENT's territory to paint, and this parent doesn't). So `margin: 1em`
+ * here and content_area's three set_margin_*(10/5/5/0) calls in
+ * KlippyGtk.py are both gone (zeroed), and this rule's `padding` moved to
+ * `.app_dialog_content` below instead, where it's applied to the widget
+ * that's actually proven to paint -- content_area now spans the entire
+ * window with no gap left for the broken layer to show through, and the
+ * equivalent inset for text/buttons comes from padding INSIDE that already
+ * -painted rectangle instead of margin outside it. */
 .dialog {
-    border: .1em solid @text-inv;
-    padding: 0.25em 2.5em;
     background-color: @text-inv;
-    margin: 1em;
+    margin: 0;
+    padding: 0;
+    /* border removed (was `.1em solid @text-inv`, 2026-07-31): it reserved
+     * ~1-2px of box-model space between content_area and the window's true
+     * edge, same class of gap as the margin/padding above -- confirmed live,
+     * a hairline strip of unpainted black remained at the bottom/right edge
+     * after zeroing margin+padding, exactly border-width sized. This border
+     * was never visible anyway once background-color started actually
+     * painting (it's the same @text-inv as the fill, zero contrast against
+     * it by design), so removing it costs nothing visually and closes the
+     * last of the gap. */
+}
+
+.app_dialog_content {
+    padding: 0.25em 2.5em;
 }
 
 .dialog button {
