# hardware/ — детали, которые изготавливаем сами

Здесь лежат **исходники деталей собственного изготовления**: лазерная резка фанеры, 3D-печать, металл под ножовку/дрель. Принцип тот же, что и у `printer-configs/`: **правится только здесь**, а не «на месте» и не в чужих облачных редакторах.

Логика разделения с закупкой простая:

- Деталь можно сделать самому → **исходник живёт в этом каталоге**, а в список закупки уходит только **сырьё** (лист фанеры, филамент, полоса алюминия).
- Деталь сделать нельзя или бессмысленно (мотор, вал, ремень, подшипник, крепёж) → **идёт в закупку целиком**, см. `docs/bom.md`.

## Правила

1. **Один каталог — одна деталь**: `hardware/<имя-детали>/`, внутри `README.md` (карточка детали) плюс файлы.
2. **Коммитим и исходник, и экспорт.** Исходник (`.scad`, `.FCStd`, `.svg`) — то, что правится; экспорт (`.stl`, `.dxf`) — то, что уходит в слайсер или на резку. Экспорт без исходника не принимается: через полгода его будет не поправить.
3. **Версии — через git, а не через имя файла.** Никаких `bracket_v2_final.stl`. Изменил — закоммитил с внятным сообщением.
4. **Деталь, изготовленная руками без модели, тоже описывается здесь** — карточкой с пометкой «оцифровать». Так уже есть переделанный кронштейн Z-концевика: он существует физически, а модели нет. Если он сломается, восстанавливать будет не по чему.
5. **Карточка обязана называть ограничения**, а не только размеры: рабочая температура, нагрузка, близость к 220 В. Для этой машины это не формальность — рядом стол на 1000 Вт и 120 °C.

## Статусы

| Метка | Значение |
|---|---|
| 💡 | Идея. Понятно, зачем деталь, не понятно, какая она |
| 📐 | Спроектировано, исходник в репозитории |
| 🔨 | Изготовлено, но не установлено или не проверено |
| ✅ | Установлено и работает |
| 🧩 | Существует физически, модели нет — **оцифровать** |

## ⚠️ Сейчас печатные детали изготовить нечем

Экструдер разобран, греть его нельзя — см. `docs/printer-status.md`, корзина C1. Хитблок и CR-Touch уже куплены (`docs/purchases.md`), но статус доставки по последним записям в `printer-status.md` — «в пути»; пересборка экструдера ждёт этого, а не самой покупки. Пока это так, **любая деталь со статусом «печать» упирается в саму машину**. Практические обходы, в порядке разумности:

1. Сделать деталь из **фанеры** (лазер или лобзик) — если геометрия плоская, это часто и жёстче, и быстрее.
2. Отдать печать на сторону.
3. Отложить до пересборки экструдера — годится только для того, что не блокирует саму пересборку.

Это же соображение — аргумент против «напечатаем крепление стола»: стол нужен собранным раньше, чем принтер начнёт печатать.

## Индекс деталей

| Деталь | Назначение | Метод | Материал | Статус |
|---|---|---|---|---|
| [`case/`](case/) | Фанерный корпус — он же несущая рама машины | лазер по фанере | фанера (толщина ⬜ не подтверждена, 5 или 6 мм — расхождение не снято) | 🧩 оцифровать |
| [`bed-adapter/`](bed-adapter/) | Переходник: стол K1 Max → рама mbot | фанера или металл (решается) | 6 мм фанера / алюминий | 💡 — см. предупреждение в самой карточке о параллельной проработке |
| `z-endstop-bracket/` | Регулируемое по высоте крепление Z-концевика | уже сделано руками | — | 🧩 оцифровать |
| `ssr-cover/` | Кожух SSR с радиатором, защита от касания 220 В | **не печать** — металл или негорючее | металл | 💡 |
| `screen-panel/` | Вырез + крепление экрана и 5 кнопок в переднюю панель | лазер по существующей панели | фанера | 💡 — сам экран и кнопки уже смонтированы и работают (2026-07-30, на Orange Pi GPIO), но это про электронику, не про эту декоративно-механическую панель; отдельный физический вырез в корпусе не подтверждён |
| `crtouch-mount/` | Кронштейн CR-Touch на каретку X | печать | PETG | 💡 (CR-Touch куплен, статус доставки — «в пути», см. `docs/bom.md` Часть 2 §14) |
| `mq2-siren-mount/` | Крепление датчика дыма и сирены | печать | PETG | 💡 — сенсор и зуммер уже смонтированы и подключены (2026-08-06), но именно печатного крепления/кожуха под них в репозитории не найдено; размещение сенсора «над принтером» пока не более чем инструкция в тексте |
| `camera-mount/` | Крепление USB-камеры на раму | печать | PETG | 💡 |
| `spool-holder/` | Держатель катушки | печать или фанера | PETG / фанера | 💡 |

**Не в этом каталоге, но та же категория «изготавливаем сами»:** крепление печатающей головы (`cad/ultra-print-head-mount.f3d` + `docs/print-head-mount.md`) — самая продвинутая деталь на 2026-08-06 (готовый печатный проект, 4 тела), но живёт вне каталога `hardware/`, конвенция сложилась позже. См. `docs/bom.md`, Часть 2 §19.

Список открытый: всё, что придётся выпиливать или печатать по ходу, заводится сюда карточкой **до** изготовления, а не после.
